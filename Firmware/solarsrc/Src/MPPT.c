#include <stdint.h>
//#include "main.h"
#include "mppt.h"
/*
uint32_t sample_pv_current(void){
		extern int countcurr;
		extern float ADCsum;

		uint32_t sampled_current;
		if (countcurr > 1000){
			sampled_current = ADCsum/1000.;
			countcurr = 0;
			ADCsum = 0;
		}
		return sampled_current; 
}

uint32_t sample_pv_voltage(void){
	uint32_t voltage_sum = 0;
	uint32_t pv_voltage_sampled = 0;
	for(int i=0; i<50; i++){
		voltage_sum += *PV_voltage;
	}
	pv_voltage_sampled = voltage_sum/50;
	return pv_voltage_sampled;
}*/




